// var const let 3 ta keyword use kora jay but always let use korbo

// template literels: carret sign (``) uer kora hoy

let country = 'Bangladesh';
let city = 'Dhaka';

console.log(`I live in ${city}, ${country}`); // this is template literels. eta string concatination diyew kora jeto but eta standard.

// spread operator: 3 ta dot( ... ) ke spread operator bole. eta array er element gula ke alada alada kore nite pare.

const numbers = [1, 2, 3, 4, 5];
console.log(numbers); // output: [1, 2, 3, 4, 5]
console.log(...numbers); // output: 1 2 3 4 5

